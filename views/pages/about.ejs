<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/about.css" />
  </head>
  <body>
    <%- include('../partials/header') %>

    <section class="about-community">
      <h1>About Our Community</h1>
      <div class="content">
        <p>
          Welcome to our community platform! We're a space for sharing,
          learning, and growing together.
        </p>
        <img src="/images/community.jpg" alt="Community Picture" />
      </div>
    </section>

    <section class="team">
      <h1>Meet the Team</h1>

      <div class="leader">
        <img src="<%= leader.image %>" alt="<%= leader.name %>" />
        <div>
          <h3><%= leader.name %></h3>
          <h4><%= leader.job %></h4>
          <h4>Role: <%= leader.role %></h4>
          <p><%= leader.bio %></p>
        </div>
      </div>

      <div class="team-members">
        <% team.forEach(member => { %>
        <div class="member">
          <img src="<%= member.image %>" alt="<%= member.name %>" />
          <h3><%= member.name %></h3>
          <h4><%= member.job %></h4>
          <h4>Role: <%= member.role %></h4>
          <p><%= member.bio %></p>
        </div>
        <% }) %>
      </div>
    </section>
    <%- include('../partials/footer') %>
  </body>
</html>
