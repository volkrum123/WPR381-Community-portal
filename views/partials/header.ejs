<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
  <header id="header">
    <a href="Home.html" class="logo">Logo</a>
    <nav class="nav">
      <% const webpages = ['home', 'about', 'events', 'thankyou', 'contact']; %>
      <% webpages.forEach(page => { %><a href="/<%= page %>" class="<%= page %>"><%=page %></a><% }) %>
    </nav>
  </header>
</body>
</html>